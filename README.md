# Delish Bites 🍽️ – Restaurant Booking System

## How to Run

### Backend
1. `cd backend`
2. `npm install`
3. Create `.env` with:
   MONGO_URI=your_mongodb_url
4. `node server.js`

### Frontend
- Open `frontend/index.html` in browser
- Make sure backend is running before login/signup
